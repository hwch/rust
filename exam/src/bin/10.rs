fn longest<'a>(x: &'a str, y: &'a str) -> &'a str {
    if x.len() > y.len() {
        x
    } else {
        y
    }
}

fn main() {
    let s1 = String::from("a");
    let s2 = "bc";

    println!("{}", longest(&s1, s2));
}
